const names = ['Felipe', 'Lucas', 'Djalma', 'Joao', 'Wesley'];
console.log(names);
const removidos = names.splice(3, 2); // Remove o elemento do array, a partir do índice 4, e remove 1 elemento

console.log(names, removidos);